[pytest]
markers =
    slow: long live tests (reg_turnin full-chain acceptance, nightly only, needs reset dev world)
# Time budgets (doctrine: mechanics fast, balance by math — see plan Wave 6):
# unit = seconds, ordinary contract = <=5 min, slow = <=10 min.
# Anything longer is a load/soak gate with rare cadence (or must be split
# into a fast mechanics test + a slow acceptance test).
